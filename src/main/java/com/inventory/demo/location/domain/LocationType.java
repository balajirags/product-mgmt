package com.inventory.demo.location.domain;

/**
 * Represents the type of a physical fulfillment location.
 */
public enum LocationType {
    WAREHOUSE,
    STORE,
    DARKSTORE,
    THREE_PL_NODE
}
